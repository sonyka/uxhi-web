---
category: Components
---

QuoteCard from web. Use via `window.UXHICon2026.QuoteCard` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Refrain

```jsx
() => (
  <div style={{ background: "var(--color-beige-30)", borderRadius: 12, padding: 8 }}>
    <QuoteCard />
  </div>
)
```
