---
category: Components
---

LogoBadge from web. Use via `window.UXHICon2026.LogoBadge` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Mark

```jsx
() => (
  <div
    style={{
      background: "var(--color-beige-30)",
      borderRadius: 12,
      padding: 24,
      width: "fit-content",
      outline: "1px dashed var(--color-gray-80)",
      outlineOffset: -24,
    }}
  >
    <LogoBadge />
  </div>
)
```
