.claude/settings.local.json
web/dist/
logs/
.vercel
.env*.local
.playwright-mcp/

# macOS
.DS_Store
**/.DS_Store

# Playwright MCP writes screenshots to the repo root
*.png.tmp
salary-*.png

# local audit output, not part of the site
orphaned-assets.html

# local artifact preview, not part of the site
_worklog-preview.html
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
web/.ds-styles.css
web/.ds-entry.tsx
web/.ds-shims/
web/.ds-tsconfig.json

# Screenshots dropped by a browser-automation review at a package root.
# Scoped to those two directories on purpose: a bare *.png would also swallow
# the real assets under web/public/ and web/src/.
/*.png
/web/*.png
